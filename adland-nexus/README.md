# AdLand Nexus

AdLand Nexus is a polished Windows desktop command center made by **AdLand Studio**.

It provides tasks, notes, snippets, focus sessions, a calculator, host system details, command search, local persistence, import/export, and a refined frameless desktop UI.

Build with npm install && npm run dist. Windows x64 releases are published automatically by GitHub Actions.