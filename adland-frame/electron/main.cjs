const{app,BrowserWindow,ipcMain,globalShortcut,nativeTheme}=require('electron');const path=require('path');let win;
function create(){win=new BrowserWindow({width:1450,height:920,minWidth:980,minHeight:650,frame:false,show:false,backgroundColor:'#0a0a0f',autoHideMenuBar:true,webPreferences:{preload:path.join(__dirname,'preload.cjs'),contextIsolation:true,nodeIntegration:false,sandbox:true}});win.loadFile(path.join(__dirname,'../src/index.html'));win.once('ready-to-show',()=>win.show())}
app.whenReady().then(()=>{nativeTheme.themeSource='dark';create();globalShortcut.register('CommandOrControl+Shift+Space',()=>{win?.show();win?.focus()})});ipcMain.handle('window',(_,a)=>{if(!win)return;if(a==='min')win.minimize();else if(a==='max')win.isMaximized()?win.unmaximize():win.maximize();else if(a==='close')win.close()});app.on('will-quit',()=>globalShortcut.unregisterAll());app.on('window-all-closed',()=>{if(process.platform!=='darwin')app.quit()});