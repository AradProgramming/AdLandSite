name: Build AdLand Frame
on:
  push:
    branches: ["release"]
    paths:
      - "adland-frame/**"
      - ".github/workflows/build-frame-windows.yml"
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: adland-frame-build
  cancel-in-progress: true
jobs:
  windows:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: adland-frame
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: npm run dist
      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: frame-v1.1.1
          name: AdLand Frame v1.1.1
          body: |
            AdLand Frame v1.1.1 — local image workspace by AdLand Studio.
            
            Windows x64 installer and portable build are attached below.
          files: |
            adland-frame/release/AdLand-Frame-1.1.1-Setup.exe
            adland-frame/release/AdLand-Frame-1.1.1-Portable.exe
          draft: false
          prerelease: false
